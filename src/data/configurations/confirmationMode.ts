/** Specifies the mode of confirmation to use when creating test files. */
export enum ConfirmationMode {
    always = "always",
    onlyIfMultiFile = "onlyIfMultiFile",
    onlyOnDirectories = "onlyOnDirectories",
    never = "never"
}